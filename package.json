{
  "name": "my-portal",
  "private": true,
  "scripts": {
    "install": "yarn lerna bootstrap",
    "clean": "yarn lerna run clean && rimraf node_modules",
    "test": "yarn lerna run test",
    "build": "yarn lerna run build",
    "start": "yarn lerna run --scope @my-portal/webapp start"
  },
  "dependencies": {
    "typescript": "^4.0.5"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.10.0",
    "@typescript-eslint/parser": "^4.10.0",
    "cross-env": "^7.0.3",
    "eslint": "^7.15.0",
    "eslint-config-prettier": "^7.0.0",
    "eslint-plugin-prettier": "^3.3.0",
    "eslint-plugin-react": "^7.21.5",
    "lerna": "^3.20.2",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2"
  }
}
