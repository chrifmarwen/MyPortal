{
  "name": "my-portal",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "clean": "lerna run clean && rimraf node_modules",
    "test": "lerna run test",
    "build": "lerna run build",
    "start": "lerna run --scope @myApp/webapp start"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "rimraf": "^3.0.2"
  }
}
